{
  "name": "sound-and-music",
  "version": "1.0.0",
  "description": "Sound and Music — phones-as-controllers party joust with a live synthesized beat.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel": "concurrently --names \"SERVER,TUNNEL\" --prefix-colors \"cyan,yellow\" \"node server.js\" \"cloudflared tunnel --url http://localhost:3000\"",
    "test": "node test/echo-test.js && node test/qr-test.js && node test/parse-join-qr-test.js && node test/integration-test.js",
    "predeploy": "echo Deploying public/ to gh-pages...",
    "deploy": "gh-pages -d public -b gh-pages"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.4",
    "gh-pages": "^6.3.0"
  }
}
